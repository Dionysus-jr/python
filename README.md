1. billing_report.py (Get the AWS bill for the last two months.)
- needs
-      sudo apt update
       sudo apt install python3 python3-pip
       pip3 install boto3 pandas matplotlib reportlab
       awscli 

